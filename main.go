package main

import (
	"github.com/1garo/go-eth/helpers"
)

func main() {
	//TODO: command to generate smart contract to deploy it later
	// abigen --sol=Store.sol --pkg=main --out=store.go
	// go run main.go store.go
	helpers.Ma()
}
